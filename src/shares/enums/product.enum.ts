export enum ProductType {
  SIM = 'SIM',
  WIFI = 'WIFI',
}

export enum ProductStatus {
  SOLD = 'SOLD',
  ACTIVE = 'ACTIVE',
  DE_ACTIVE = 'DE_ACTIVE',
  LOCKED = 'LOCKED',
}

export enum PartnerType {
  LOYAL_CUSTOMER = 'SOLD',
}
